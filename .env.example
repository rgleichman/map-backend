# Map tiles (https://www.maptiler.com/)
export MAPTILER_API_KEY=your_key_here

# HMAC secret for email login identifiers (use mix phx.gen.secret); required in production
export EMAIL_IDENTIFIER_SECRET=
